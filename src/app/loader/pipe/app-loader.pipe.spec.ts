import { AppLoaderPipe } from './app-loader.pipe';

describe('AppLoaderPipe', () => {
  it('create an instance', () => {
    const pipe = new AppLoaderPipe();
    expect(pipe).toBeTruthy();
  });
});
